feat(v2.7): CPU instruction emulation, DualSense HID, bandwidth-aware LOD, MMIO/NVMe LLE, RT on-ramp, EXIT diagnostics

Verified building on Windows (clang-cl) and Linux (clang++ 18, WSL).

- CPU: emulate RDTSC/RDTSCP, CPUID (hypervisor leaves), XGETBV/XSETBV,
  RDMSR/WRMSR, RDPMC/RDPRU/RDPID, CLZERO, WBINVD/INVD, MWAIT,
  LLDT/LTR/STR/LMSW/SMSW in TryEmulate (no-regression additive)
- DualSense: native HID driver (input reports + rumble/lightbar/adaptive
  triggers) wired into real pad path (spec-accurate, unvalidated on hardware)
- Bandwidth-aware adaptive LOD bias controller for iGPUs (mechanism
  self-validatable; game benefit unvalidated)
- MMIO bus subsystem + NVMe LLE device on real MMU (infrastructure,
  not exercised by games yet)
- RT on-ramp: wire extension loading into device creation (enablement only)
- EXIT diagnostics: 24 guards upgraded to descriptive messages
- Release notes: RELEASE_v2.7.md (honest framing)